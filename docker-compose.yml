version: "3"
reverseproxy:
    image: nginx
    depends_on: 
        - frontend
    volumes:
        - ./nginx.conf:/etc/nginx/conf.d/default.conf
        ports:
        - "80:80"

networks:
    default:
       external:
          name: allnetwork