version: "3"
reverseproxy:
    image: nginx
    volumes:
        - ./nginx.conf:/etc/nginx/conf.d/default.conf
        ports:
        - "80:80"

networks:
    default:
       external:
          name: allnetwork